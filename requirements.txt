# Percenty 프로젝트 의존성

# 웹 자동화
selenium>=4.0.0
webdriver-manager>=3.8.0

# GUI
tkinter  # Python 기본 포함

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# 이미지 처리 및 OCR
pillow>=9.0.0
easyocr>=1.7.0
opencv-python>=4.8.0

# 유틸리티
requests>=2.28.0
pyautogui>=0.9.54
psutil>=5.9.0
pyperclip>=1.8.2

# 주기적 실행을 위한 스케줄링
schedule>=1.2.0

# 로깅 및 설정
pyyaml>=6.0

# 개발 도구 (선택사항)
pytest>=7.0.0
pylint>=2.15.0
black>=22.0.0
mypy>=0.991
flake8>=5.0.0

# 추가 유틸리티 (선택사항)
colorama>=0.4.6  # Windows 터미널 색상 지원
tqdm>=4.65.0     # 진행률 표시
chardet>=5.1.0   # 문자 인코딩 감지

# 시스템별 추가 의존성 (필요시 설치)
# Windows: pywin32>=306
# macOS: pyobjc-framework-Cocoa>=9.0
# Linux: python3-tk (시스템 패키지)